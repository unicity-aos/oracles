schema-version = 1

[runtime]
repository = "astrid-runtime/astrid"
version = "2026.9.1"
tag = "v2026.9.1"
version-requirement = ">=2026.9.1"
release-workflow-identity = "https://github.com/astrid-runtime/astrid/.github/workflows/release.yml@refs/tags/v2026.9.1"
release-ready = true

# version/tag identify the published Astrid floor this Oracle release documents.
# version-requirement is the minimum compatible installed runtime. Authenticate
# the actual installed artifact from its own signed AOS release metadata.
# Never point a release build at a moving channel.
